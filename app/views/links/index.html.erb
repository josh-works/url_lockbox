<%= render partial: "partials/add_new_link" %>

<div class="links_list">
  <% @links.each do |link| %>
    <div class="link_item">
      <h3><%= link.title %></h3>
      <%= link_to link.url %>
      <p>
        Read status? <%= link.read %>
      </p>
      <%= button_to "Mark Link as Read", edit_link_path(link) %>
      <%= link_to "Edit Link", edit_link_path(link) %>
    </div>
  <% end %>
</div>
