You've made it to root

<div class="links_list">
  <% @links.each do |link| %>
    <div class="link_item">
      <h3><%= link.title %></h3>
      <%= link_to link.url %>
      <p>
        Read status? <%= link.read %>
      </p>
    </div>
  <% end %>

</div>
