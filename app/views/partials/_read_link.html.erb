<% if link.read %>
  <div class="link_item read" data-link-id="<%= link.id %>" data-link-read="true">
<% else %>
  <div class="link_item" data-link-id="<%= link.id %>" data-link-read="false">
<% end %>
